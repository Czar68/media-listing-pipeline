import type { MediaListingPipelineInput } from '@media-listing/media-listing-pipeline';

/** Canonical deterministic pipeline input for repo-root export seams. */
export type MediaListingExecutionFixtureInput = MediaListingPipelineInput;
